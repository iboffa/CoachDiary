import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.coachdiary.app',
  appName: 'CoachDiary',
  webDir: 'dist/renderer/browser',
};

export default config;
